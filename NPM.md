NPM modules availables for JS 
```yaml
merge2: "Merge multiple streams into one stream in sequence or parallel."
ajv-formats: "Format validation for Ajv v7+"
unpipe: "Unpipe a stream from all destinations"
cron-parser: "Node.js library for parsing crontab instructions"
upper-case-first: "Transforms the string with the first character in upper cased"
n8n-nodes-base: "Base nodes of n8n"
xregexp: "Extended JavaScript regular expressions"
worker-timers: "A replacement for setInterval() and setTimeout() which works in unfocused windows."
dingbat-to-unicode: "Mapping from Dingbat fonts, such as Symbol, Webdings and Wingdings, to Unicode code points"
ansi-regex: "Regular expression for matching ANSI escape codes"
stream-shift: "Returns the next buffer/object in a stream's readable queue"
md5: "js function for hashing messages with MD5"
recast: "JavaScript syntax tree transformer, nondestructive pretty-printer, and automatic source map generator"
sax: "An evented streaming XML parser in JavaScript"
xml2js: "Simple XML to JavaScript object converter."
ics: "iCal (ics) file generator"
minipass: "minimal implementation of a PassThrough stream"
unique-filename: "Generate a unique filename for use in temporary directories or caches."
strip-json-comments: "Strip comments from JSON. Lets you use comments in your JSON files!"
http-cache-semantics: "Parses Cache-Control and other headers. Helps building correct HTTP caches and proxies"
python-struct: "Packs/Unpacks/Measures structs according to Python's `struct` format"
dotenv: "Loads environment variables from .env file"
sqlstring: "Simple SQL escape and format for MySQL"
jackspeak: "A very strict and proper argument parser."
which-boxed-primitive: "Which kind of boxed JS primitive is this?"
ssri: "Standard Subresource Integrity library -- parses, serializes, generates, and verifies integrity metadata according to the SRI spec."
generate-function: "Module that helps you write generated functions in Node"
safe-regex-test: "Give a regex, get a robust predicate function that tests it against a string."
core-util-is: "The `util.is*` functions introduced in Node v0.12."
mime-types: "The ultimate javascript content-type utility."
split2: "split a Text Stream into a Line Stream, using Stream 3"
forwarded-parse: "Parse the Forwarded header (RFC 7239) into an array of objects"
eslint-config-riot: " Riot default eslint config file"
to-regex-range: "Pass two numbers, get a regex-compatible source string for matching ranges. Validated against more than 2.78 million test assertions."
inherits: "Browser-friendly inheritance fully compatible with standard node.js inherits()"
mailsplit: "Split email messages into an object stream"
safe-push-apply: "Push an array of items into an array, while being robust against prototype modification"
typed-array-buffer: "Get the ArrayBuffer out of a TypedArray, robustly."
raw-body: "Get and validate the raw body of a readable stream."
micromatch: "Glob matching for javascript/node.js. A replacement and faster alternative to minimatch and multimatch."
acorn-walk: "ECMAScript (ESTree) AST walker"
express-prom-bundle: "express middleware with popular prometheus metrics in one bundle"
nanoclone: "300B to deep clone JavaScript objects"
yargs: "yargs the modern, pirate-themed, successor to optimist."
mongodb-connection-string-url: "MongoDB connection strings, based on the WhatWG URL API"
shebang-regex: "Regular expression for matching a shebang line"
camelcase: "Convert a dash/dot/underscore/space separated string to camelCase or PascalCase: `foo-bar` → `fooBar`"
is-arguments: "Is this an arguments object? It's a harder question than you think."
jq: error (at /usr/local/lib/node_modules/n8n/node_modules/append-field/package.json:19): null (null) cannot be matched, as it is not a string
infisical-node: ""
crlf-normalize: "Detect and Normalize the newline characters"
parse5: "HTML parser and serializer."
getpass: "getpass for node.js"
commander: "the complete solution for node.js command-line programs"
package-json-from-dist: "Load the local package.json from either src or dist folder"
path-scurry: "walk paths fast and efficiently"
xml: "Fast and simple xml generator. Supports attributes, CDATA, etc. Includes tests and examples."
alasql: "Use SQL to select and filter javascript data - including relational joins and search in nested objects (JSON). Export to and import from Excel and CSV"
assert: "The assert module from Node.js, for the browser."
proto3-json-serializer: "Support for proto3 JSON serialiazation/deserialization for protobuf.js"
assert-options: "Generic options parameter handling."
whatwg-url: "An implementation of the WHATWG URL Standard's URL API and parsing machinery"
standard-as-callback: "A performant and standard (Bluebird) library that registers a node-style callback on a promise"
string-width: "Get the visual width of a string - the number of columns required to display it"
pg-int8: "64-bit big-endian signed integer-to-string conversion"
js-md4: "A simple MD4 hash function for JavaScript supports UTF-8 encoding."
data-view-byte-offset: "Get the byteOffset out of a DataView, robustly."
nodemailer: "Easy as cake e-mail sending from your Node.js applications"
chownr: "like `chown -R`"
lilconfig: "A zero-dependency alternative to cosmiconfig"
get-port: "Get an available port"
json-bigint: "JSON.parse with bigints support"
buffer: "Node.js Buffer API, for the browser"
strip-ansi: "Strip ANSI escape codes from a string"
lodash.clonedeep: "The lodash method `_.cloneDeep` exported as a module."
send: "Better streaming static file server with Range and conditional-GET support"
abort-controller: "An implementation of WHATWG AbortController interface."
object-hash: "Generate hashes from javascript objects in node and the browser."
xpath: "DOM 3 XPath implemention and helper for node.js."
string.prototype.trimstart: "ES2019 spec-compliant String.prototype.trimStart shim."
node-fetch: "A light-weight module that brings window.fetch to node.js"
cheerio: "The fast, flexible & elegant library for parsing and manipulating HTML and XML."
string_decoder: "The string_decoder module from Node core"
dir-glob: "Convert directories to glob compatible strings"
seq-queue: "A simple tool to keep requests to be executed in order."
cliui: "easily create complex multi-column command-line-interfaces"
is-nan: "ES2015-compliant shim for Number.isNaN - the global isNaN returns false positives."
hasown: "A robust, ES3 compatible, \"has own property\" predicate."
minimalistic-assert: "minimalistic-assert ==="
dayjs: "2KB immutable date time library alternative to Moment.js with the same modern API "
escape-html: "Escape string for use in HTML"
strtok3: "A promise based streaming tokenizer"
uid-safe: "URL and cookie safe UIDs"
clean-stack: "Clean up error stack traces"
tsscmp: "Timing safe string compare using double HMAC"
form-data-encoder: "Encode FormData content into the multipart/form-data format"
glob: "the most correct and second fastest glob implementation in JavaScript"
delayed-stream: "Buffers events from a stream until you are ready to handle them."
set-proto: "Robustly set the [[Prototype]] of an object"
is-plain-object: "Returns true if an object was created by the `Object` constructor, or Object.create(null)."
gcp-metadata: "Get the metadata from a Google Cloud Platform environment"
js-nacl: "High-level API to libsodium."
spex: "Specialized Promise Extensions"
process-nextick-args: "process.nextTick but always with args"
path-type: "Check if a path is a file, directory, or symlink"
bn.js: "Big number implementation in pure javascript"
pkce-challenge: "Generate or verify a Proof Key for Code Exchange (PKCE) challenge pair"
json-diff: "JSON diff"
tar-stream: "tar-stream is a streaming tar parser and generator and nothing else. It is streams2 and operates purely using streams which means you can easily extract/parse tarballs without ever hitting the file system."
agentkeepalive: "Missing keepalive http.Agent"
safer-buffer: "Modern Buffer API polyfill without footguns"
strip-ansi: "Strip ANSI escape codes from a string"
supports-preserve-symlinks-flag: "Determine if the current node version supports the `--preserve-symlinks` flag."
gaxios: "A simple common HTTP client specifically for Google APIs and services."
color-string: "Parser and generator for CSS color strings"
minipass-fetch: "An implementation of window.fetch in Node.js using Minipass streams"
ecc-jsbn: "ECC JS code based on JSBN"
asap: "High-priority task queue for Node.js and browsers"
reinterval: "reschedulable setInterval for node.js"
ieee754: "Read/write IEEE754 floating point numbers from/to a Buffer or array-like object"
langsmith: "Client library to connect to the LangSmith LLM Tracing and Evaluation Platform."
iso-639-1: "ISO-639-1 codes"
lower-case: "Transforms the string to lower case"
esprima: "ECMAScript parsing infrastructure for multipurpose analysis"
object-keys: "An Object.keys replacement, in case Object.keys is not available. From https://github.com/es-shims/es5-shim"
get-symbol-description: "Gets the description of a Symbol. Handles `Symbol()` vs `Symbol('')` properly when possible."
upper-case: "Transforms the string to upper case"
change-case: "Transform a string between `camelCase`, `PascalCase`, `Capital Case`, `snake_case`, `param-case`, `CONSTANT_CASE` and others"
array-flatten: "Flatten an array of nested arrays into a single flat array"
yocto-queue: "Tiny queue data structure"
jq: error (at /usr/local/lib/node_modules/n8n/node_modules/stoppable/package.json:49): null (null) cannot be matched, as it is not a string
is-property: "Tests if a JSON property can be accessed using . syntax"
zod-class: "Create classes from Zod Object schemas all in one line"
parse-passwd: "Parse a passwd file into a list of users."
delegates: "delegate methods and accessors to another property"
is-map: "Is this value a JS Map? This module works cross-realm/iframe, and despite ES6 @@toStringTag."
decamelize: "Convert a camelized string into a lowercased one with a custom separator: unicornRainbow → unicorn_rainbow"
node-addon-api: "Node.js API (Node-API)"
is-async-function: "Determine if a function is a native async function."
p-finally: "`Promise#finally()` ponyfill - Invoked when the promise is settled regardless of outcome"
decimal.js: "An arbitrary-precision Decimal type for JavaScript."
tough-cookie: "RFC6265 Cookies and Cookie Jar for node.js"
asn1.js: "ASN.1 encoder and decoder"
promise-inflight: "One promise for multiple requests in flight to avoid async duplication"
queue-microtask: "fast, tiny `queueMicrotask` shim for modern engines"
swagger-ui-express: "Swagger UI Express"
lodash.camelcase: "The lodash method `_.camelCase` exported as a module."
jq: error (at /usr/local/lib/node_modules/n8n/node_modules/cohere-ai/package.json:49): null (null) cannot be matched, as it is not a string
are-we-there-yet: "Keep track of the overall completion of many disparate processes"
csv-parse: "CSV parsing implementing the Node.js `stream.Transform` API"
promise-retry: "Retries a function that returns a promise, leveraging the power of the retry module."
random-bytes: "URL and cookie safe UIDs"
encoding: "Convert encodings, uses iconv-lite"
base64-js: "Base64 encoding/decoding in pure JS"
p-cancelable: "Create a promise that can be canceled"
pg-promise: "PostgreSQL interface for Node.js"
ajv-draft-04: "Ajv class for JSON Schema draft-04"
bytes: "Utility to parse a string bytes to bytes and vice-versa"
libmime: "Encode and decode quoted printable and base64 strings"
currency-codes: "Lookup currency codes based on ISO 4217"
ansi-styles: "ANSI escape codes for styling strings in the terminal"
amqplib: "An AMQP 0-9-1 (e.g., RabbitMQ) library and client."
nanoid: "A tiny (116 bytes), secure URL-friendly unique string ID generator"
process: "process information for node.js and browsers"
resolve: "resolve like require.resolve() on behalf of files asynchronously and synchronously"
gopd: "`Object.getOwnPropertyDescriptor`, but accounts for IE's broken implementation."
handlebars: "Handlebars provides the power necessary to let you build semantic templates effectively with no frustration"
wrappy: "Callback wrapping utility"
pdfjs-dist: "Generic build of Mozilla's PDF.js library."
proxy-from-env: "Offers getProxyForUrl to get the proxy URL for a URL, respecting the *_PROXY (e.g. HTTP_PROXY) and NO_PROXY environment variables."
jws: "Implementation of JSON Web Signatures"
msgpackr-extract: "Node addon for string extraction for msgpackr"
google-p12-pem: "Convert Google .p12 keys to .pem keys."
mimic-response: "Mimic a Node.js HTTP response stream"
dot-case: "Transform into a lower case string with a period between words"
pg: "PostgreSQL client - pure javascript & libpq with the same API"
side-channel: "Store information about any JS value in a side channel. Uses WeakMap if available."
openai: "The official TypeScript library for the OpenAI API"
on-finished: "Execute a callback when a request closes, finishes, or errors"
cookie-parser: "Parse HTTP request cookies"
whatwg-encoding: "Decode strings according to the WHATWG Encoding Standard"
prebuild-install: "A command line tool to easily install prebuilt binaries for multiple version of node/iojs on a specific platform"
on-headers: "Execute a listener when a response is about to write headers"
node-forge: "JavaScript implementations of network transports, cryptography, ciphers, PKI, message digests, and various utilities."
tlds: "A list of TLDs."
ssh2-sftp-client: "ssh2 sftp client for node"
vary: "Manipulate the HTTP Vary header"
postgres-date: "Postgres date column parser"
picocolors: "The tiniest and the fastest library for terminal output formatting with ANSI colors"
lodash.isboolean: "The lodash method `_.isBoolean` exported as a module."
is-arrayish: "Determines if an object can be used as an array"
fn.name: "Extract names from functions"
ip-address: "A library for parsing IPv4 and IPv6 IP addresses in node and the browser."
array-parallel: "Call an array of asynchronous functions in parallel"
fast-glob: "It's a very fast and efficient glob library for Node.js"
property-expr: "tiny util for getting and setting deep object props safely"
color: "Color conversion and manipulation with CSS string support"
socks-proxy-agent: "A SOCKS proxy `http.Agent` implementation for HTTP and HTTPS"
sanitize-html: "Clean up user-submitted HTML, preserving allowlisted elements and allowlisted attributes on a per-element basis"
etag: "Create simple HTTP ETags"
define-lazy-prop: "Define a lazily evaluated property on an object"
encodeurl: "Encode a URL to a percent-encoded form, excluding already-encoded sequences"
is-symbol: "Determine if a value is an ES6 Symbol or not."
asn1: "Contains parsers and serializers for ASN.1 (currently BER only)"
p-map: "Map over promises concurrently"
es-to-primitive: "ECMAScript “ToPrimitive” algorithm. Provides ES5 and ES2015 versions."
langchain: "Typescript bindings for langchain"
yamljs: "Standalone JavaScript YAML 1.2 Parser & Encoder. Works under node.js and all major browsers. Also brings command line YAML/JSON conversion tools."
lodash-es: "Lodash exported as ES modules."
array-buffer-byte-length: "Get the byte length of an ArrayBuffer, even in engines without a `.byteLength` method."
quoted-printable: "A robust & character encoding–agnostic JavaScript implementation of the `Quoted-Printable` content transfer encoding as defined by RFC 2045."
bcryptjs: "Optimized bcrypt in plain JavaScript with zero dependencies. Compatible to 'bcrypt'."
set-function-name: "Set a function's name property"
axios-retry: "Axios plugin that intercepts failed requests and retries them whenever posible."
node-abort-controller: "AbortController for Node based on EventEmitter"
pgpass: "Module for reading .pgpass"
is-core-module: "Is this specifier a node.js core module?"
url-value-parser: "extracts and replaces values and IDs in URLs"
triple-beam: "Definitions of levels for logging purposes & shareable Symbol constants."
https-proxy-agent: "An HTTP(s) proxy `http.Agent` implementation for HTTPS"
has-property-descriptors: "Does the environment have full property descriptor support? Handles IE 8's broken defineProperty/gOPD."
interpret: "A dictionary of file extensions and associated module loaders."
own-keys: "Robustly get an object's own property keys (strings and symbols), including non-enumerables when possible"
adm-zip: "Javascript implementation of zip for nodejs with support for electron original-fs. Allows user to create or extract zip files both in memory or to/from disk"
is-glob: "Returns `true` if the given string looks like a glob pattern or an extglob pattern. This makes it easy to create code that only uses external modules like node-glob when necessary, resulting in much faster code execution and initialization time, and a better user experience."
node-html-markdown: "Fast HTML to markdown cross-compiler, compatible with both node and the browser"
is-finalizationregistry: "Is this value a JS FinalizationRegistry? This module works cross-realm/iframe, and despite ES6 @@toStringTag."
rndm: "random string generator"
libqp: "Encode and decode quoted-printable strings according to rfc2045"
object.assign: "ES6 spec-compliant Object.assign shim. From https://github.com/es-shims/es6-shim"
bluebird: "Full featured Promises/A+ implementation with exceptionally good performance"
ajv: "Another JSON Schema Validator"
typed-array-byte-length: "Robustly get the byte length of a Typed Array"
console-control-strings: "A library of cross-platform tested terminal/console command strings for doing things like color and cursor positioning.  This is a subset of both ansi and vt100.  All control codes included work on both Windows & Unix-like OSes, except where noted."
strict-event-emitter-types: "Strictly and safely type any EventEmitter-like interface on any object"
colors: "get colors in your node.js console"
syslog-client: "TCP and UDP syslog client RFC 5424 & RFC 3164"
cross-spawn: "Cross platform child_process#spawn and child_process#spawnSync"
cpu-features: "A simple binding to Google's cpu_features library for obtaining information about installed CPU(s)"
enabled: "Check if a certain debug flag is enabled."
string.prototype.trimend: "ES2019 spec-compliant String.prototype.trimEnd shim."
he: "A robust HTML entities encoder/decoder with full Unicode support."
toml: "TOML parser for Node.js (parses TOML spec v0.4.0)"
bl: "Buffer List: collect buffers and access with a standard readable Buffer interface, streamable too!"
depd: "Deprecate all the things"
napi-build-utils: "A set of utilities to assist developers of tools that build N-API native add-ons"
import-in-the-middle: "Intercept imports in Node.js"
jq: error (at /usr/local/lib/node_modules/n8n/node_modules/swagger-ui-dist/package.json:20): null (null) cannot be matched, as it is not a string
ejs: "Embedded JavaScript templates"
jsbn: "The jsbn library is a fast, portable implementation of large-number math in pure JavaScript, enabling public-key crypto and other applications on desktop and mobile browsers."
call-bind-apply-helpers: "Helper functions around Function call/apply/bind, for use in `call-bind`"
js-yaml: "YAML 1.2 parser and serializer"
default-browser-id: "Get the bundle identifier of the default browser (macOS). Example: com.apple.Safari"
arraybuffer.prototype.slice: "ES spec-compliant shim for ArrayBuffer.prototype.slice"
samlify: "High-level API for Single Sign On (SAML 2.0)"
node-ensure: "Async module-loading library and protocol for bundlers/loaders targeting isomorphic apps and Node.js."
y18n: "the bare-bones internationalization library used by yargs"
internal-slot: "ES spec-like internal slots"
pg-protocol: "The postgres client/server binary protocol, implemented in TypeScript"
socks: "Fully featured SOCKS proxy client supporting SOCKSv4, SOCKSv4a, and SOCKSv5. Includes Bind and Associate functionality."
set-blocking: "set blocking stdio and stderr ensuring that terminal output does not truncate"
shell-escape: "Escape and stringify an array of arguments to be executed on the shell"
postcss: "Tool for transforming styles with JS plugins"
pako: "zlib port to javascript - fast, modularized, with browser support"
wrap-ansi: "Wordwrap a string with ANSI escape codes"
has-unicode: "Try to guess if your terminal supports unicode"
widest-line: "Get the visual width of the widest line in a string - the number of columns required to display it"
is-fullwidth-code-point: "Check if the character represented by a given Unicode code point is fullwidth"
node-machine-id: "Unique machine (desktop) id (no admin privileges required)."
mime: "A comprehensive library for mime-type mapping"
universalify: "Make a callback- or promise-based function support both promises and callbacks."
encoding-japanese: "Convert or detect character encoding in JavaScript"
json-schema-traverse: "Traverse JSON Schema passing each schema object to callback"
xmlchars: "Utilities for determining if characters belong to character classes defined by the XML specs."
argparse: "CLI arguments parser. Native port of python's argparse."
source-map-support: "Fixes stack traces for files with source maps"
is-typed-array: "Is this value a JS Typed Array? This module works cross-realm/iframe, does not depend on `instanceof` or mutable properties, and despite ES6 Symbol.toStringTag."
colorspace: "Generate HEX colors for a given namespace."
dashdash: "A light, featureful and explicit option parsing library."
npmlog: "logger for npm"
data-view-byte-length: "Get the byteLength out of a DataView, robustly."
lodash.isstring: "The lodash method `_.isString` exported as a module."
range-parser: "Range header field string parser"
require-directory: "Recursively iterates over specified directory, require()'ing each file, and returning a nested hash structure containing those modules."
aproba: "A ridiculously light-weight argument validator (now browser friendly)"
gtoken: "Node.js Google Authentication Service Account Tokens"
get-proto: "Robustly get the [[Prototype]] of an object"
color-convert: "Plain color conversion functions"
merge-descriptors: "Merge objects using descriptors"
js-sdsl: "javascript standard data structure library which benchmark against C++ STL"
is-inside-container: "Check if the process is running inside a container (Docker/Podman)"
balanced-match: "Match balanced character pairs, like \"{\" and \"}\""
libphonenumber-js: "A simpler (and smaller) rewrite of Google Android's libphonenumber library in javascript"
heap: "binary heap (priority queue) algorithms (ported from Python's heapq module)"
escape-string-regexp: "Escape RegExp special characters"
aggregate-error: "Create an error from multiple errors"
tunnel: "Node HTTP/HTTPS Agents for tunneling proxies"
dom-serializer: "render domhandler DOM nodes to a string"
type-detect: "Improved typeof detection for node.js and the browser."
google-logging-utils: "An ad-hoc debug logger package for other libraries"
bindings: "Helper module for loading your native module's .node file"
tweetnacl: "Port of TweetNaCl cryptographic library to JavaScript"
fast-text-encoding: "Fast polyfill for TextEncoder and TextDecoder, only supports utf-8"
available-typed-arrays: "Returns an array of Typed Array names that are available in the current environment"
cluster-key-slot: "Generates CRC hashes for strings - for use by node redis clients to determine key slots."
sparse-bitfield: "Bitfield that allocates a series of small buffers to support sparse bits without allocating a massive buffer"
one-time: "Run the supplied function exactly one time (once)"
nub: "Uniqueness functions"
forwarded: "Parse HTTP X-Forwarded-For header"
setprototypeof: "A small polyfill for Object.setprototypeof"
csrf: "primary logic behind csrf tokens"
postgres-bytea: "Postgres bytea parser"
lodash.isplainobject: "The lodash method `_.isPlainObject` exported as a module."
playwright-core: "A high-level API to automate web browsers"
es-aggregate-error: "ES Proposal spec-compliant shim for AggregateError"
groq-sdk: "The official TypeScript library for the Groq API"
picomatch: "Blazing fast and accurate glob matcher written in JavaScript, with no dependencies and full support for standard and extended Bash glob features, including braces, extglobs, POSIX brackets, and regular expressions."
yargs-parser: "the mighty option parser used by yargs"
commist: "Build your commands on minimist!"
es-define-property: "`Object.defineProperty`, but not IE 8's broken one."
array-union: "Create an array of unique values, in order, from the input arrays"
is-extglob: "Returns true if a string has an extglob."
is-callable: "Is this JS value callable? Works with Functions and GeneratorFunctions, despite ES6 @@toStringTag."
querystringify: "Querystringify - Small, simple but powerful query string parser."
pg-cloudflare: "A socket implementation that can run on Cloudflare Workers using native TCP connections."
is-bigint: "Is this value an ES BigInt?"
lodash.isnumber: "The lodash method `_.isNumber` exported as a module."
long: "A Long class for representing a 64-bit two's-complement integer value."
escalade: "A tiny (183B to 210B) and fast utility to ascend parent directories"
lossless-json: "Parse JSON without risk of losing numeric information"
simple-git: "Simple GIT interface for node.js"
color-support: "A module which will endeavor to guess your terminal's level of color support."
node-gyp-build-optional-packages: "Build tool and bindings loader for node-gyp that supports prebuilds"
aws-ssl-profiles: "AWS RDS SSL certificates bundles."
readdirp: "Recursive version of fs.readdir with small RAM & CPU footprint."
eastasianwidth: "Get East Asian Width from a character."
buffer-more-ints: "Add support for more integer widths to Buffer"
es-object-atoms: "ES Object-related atoms: Object, ToObject, RequireObjectCoercible"
regexp.prototype.flags: "ES6 spec-compliant RegExp.prototype.flags shim."
base-64: "A robust base64 encoder/decoder that is fully compatible with `atob()` and `btoa()`, written in JavaScript."
get-intrinsic: "Get and robustly cache all JS language-level intrinsics at first require time"
unique-slug: "Generate a unique character string suitible for use in files and URLs."
saxes: "An evented streaming XML parser in JavaScript"
cjs-module-lexer: "Lexes CommonJS modules, returning their named exports metadata"
underscore: "JavaScript's functional programming helper library."
logform: "An mutable object-based log format designed for chaining & objectMode streams."
readable-stream: "Node.js Streams, a user-land copy of the stream library from Node.js"
minipass-sized: "A Minipass stream that raises an error if you get a different number of bytes than expected"
unbox-primitive: "Unbox a boxed JS primitive value."
reusify: "Reuse objects and functions with style"
p-limit: "Run multiple promise-returning & async functions with limited concurrency"
chardet: "Character encoding detector"
for-each: "A better forEach"
winston: "A logger for just about everything."
fast-deep-equal: "Fast deep equal"
generate-schema: "Effortlessly convert your JSON Object to JSON Schema, Mongoose Schema, or a Generic template for quick documentation / upstart."
axios: "Promise based HTTP client for the browser and node.js"
globby: "User-friendly glob matching"
slash: "Convert Windows backslash paths to slash paths"
linkify-it: "Links recognition library with FULL unicode support"
uglify-js: "JavaScript parser, mangler/compressor and beautifier toolkit"
asn1.js-rfc2560: "RFC2560 structures for asn1.js"
isbot: "🤖 detect bots/crawlers/spiders via the user agent."
formidable: "A node.js module for parsing form data, especially file uploads."
pg-types: "Query result type converters for node-postgres"
mammoth: "Convert Word documents from docx to simple HTML and Markdown"
imurmurhash: "An incremental implementation of MurmurHash3"
typescript: "TypeScript is a language for application scale JavaScript development"
capital-case: "Transform into a space separated string with each word capitalized"
named-placeholders: "sql named placeholders to unnamed compiler"
isarray: "Array#isArray for older browsers"
dommatrix: "ES6+ shim for DOMMatrix"
proxy-addr: "Determine address of proxied request"
object-is: "ES2015-compliant shim for Object.is - differentiates between -0 and +0"
iconv-lite: "Convert character encodings in pure javascript."
html-entities: "Fastest HTML entities encode/decode library."
es-abstract: "ECMAScript spec abstract operations."
fflate: "High performance (de)compression in an 8kB package"
mkdirp-classic: "Mirror of mkdirp 0.5.2"
make-dir: "Make a directory and its parents if needed - Think `mkdir -p`"
nth-check: "Parses and compiles CSS nth-checks to highly optimized functions."
tdigest: "javascript implementation of Dunning's T-Digest for streaming quantile approximation"
tr46: "An implementation of the Unicode TR46 spec"
jsonwebtoken: "JSON Web Token implementation (symmetric and asymmetric)"
graceful-fs: "A drop-in replacement for fs, making various improvements."
rusha: "A high-performance pure-javascript SHA1 implementation suitable for large binary data."
rimraf: "A deep deletion module for node (like `rm -rf`)"
option: "The option type, also known as the maybe type, for JavaScript"
bintrees: "Binary Search Trees"
xml-crypto: "Xml digital signature and encryption library for Node.js"
node-abi: "Get the Node ABI for a given target and runtime, and vice versa."
xmlbuilder: "An XML builder for node.js"
jake: "JavaScript build tool, similar to Make or Rake"
bson: "A bson parser for node.js and the browser"
peek-readable: "Read and peek from a readable stream"
minipass-pipeline: "create a pipeline of streams using Minipass"
lodash.defaults: "The lodash method `_.defaults` exported as a module."
path-to-regexp: "Express style path to RegExp utility"
lie: "A basic but performant promise implementation"
tmp-promise: "The tmp package with promises support and disposers."
ast-types: "Esprima-compatible implementation of the Mozilla JS Parser API"
xss: "Sanitize untrusted HTML (to prevent XSS) with a configuration specified by a Whitelist"
first-match: "Find the first element in an array that passes a callback test. Equivalent to underscore.find()"
htmlparser2: "Fast & forgiving HTML/XML parser"
debug: "Lightweight debugging utility for Node.js and the browser"
kafkajs: "A modern Apache Kafka client for node.js"
native-duplexpair: "Make a full duplex stream with 2 Duplex endpoints, using the native version of `Duplex`"
path-case: "Transform into a lower case string with slashes between words"
utils-merge: "merge() utility function"
github-from-package: "return the github url from a package.json file"
date-fns: "Modern JavaScript date utility library"
dunder-proto: "If available, the `Object.prototype.__proto__` accessor and mutator, call-bound"
expand-template: "Expand placeholders in a template string"
express-handlebars: "A Handlebars view engine for Express which doesn't suck."
call-bound: "Robust call-bound JavaScript intrinsics, using `call-bind` and `get-intrinsic`."
buildcheck: "Build environment checking (a la autoconf) for node.js"
indent-string: "Indent each line in a string"
ollama: "Ollama Javascript library"
acorn: "ECMAScript parser"
stream-events: "Get an event when you're being sent data or asked for it."
detect-libc: "Node.js module to detect the C standard library (libc) implementation family and version"
domutils: "Utilities for working with htmlparser2's dom"
arrify: "Convert a value to an array"
web-streams-polyfill: "Web Streams, based on the WHATWG spec reference implementation"
constant-case: "Transform into upper case string with an underscore between words"
stack-trace: "Get v8 stack traces as an array of CallSite objects."
content-type: "Create and parse HTTP Content-Type header"
wrap-ansi: "Wordwrap a string with ANSI escape codes"
retry-axios: "Retry HTTP requests with Axios."
node-gyp: "Node.js native addon build tool"
utf7: "Converts text to and from UTF-7 (RFC 2152 and IMAP)"
jq: error (at /usr/local/lib/node_modules/n8n/node_modules/jsbi/package.json:35): null (null) cannot be matched, as it is not a string
domelementtype: "all the types of nodes in htmlparser2's dom"
simple-swizzle: "Simply swizzle your arguments"
charenc: "character encoding utilities"
set-function-length: "Set a function's length property"
entities: "Encode & decode XML and HTML entities with ease & speed"
xmllint-wasm: "Fork of https://www.npmjs.com/package/xmllint with some updates"
readable-web-to-node-stream: "Converts a Web-API readable-stream into a Node readable-stream."
retry: "Abstraction for exponential and custom retry strategies for failed operations."
minifaker: "lightweight faker.js"
title-case: "Transform a string into title case following English rules"
parse5-htmlparser2-tree-adapter: "htmlparser2 tree adapter for parse5."
requires-port: "Check if a protocol requires a certain port number to be added to an URL."
typed-array-byte-offset: "Robustly get the byte offset of a Typed Array"
w3c-xmlserializer: "A per-spec XML serializer implementation"
webidl-conversions: "Implements the WebIDL algorithms for converting to and from JavaScript values"
braces: "Bash-like brace expansion, implemented in JavaScript. Safer than other brace expansion libs, with complete support for the Bash 4.3 braces specification, without sacrificing speed."
dreamopt: "Command-line parser with readable syntax from your sweetest dreams"
nopt: "Option parsing for Node, supporting types, shorthands, etc. Used by npm."
minipass-collect: "A Minipass stream that collects all the data into a single chunk"
open: "Open stuff like URLs, files, executables. Cross-platform."
ipaddr.js: "A library for manipulating IPv4 and IPv6 addresses in JavaScript."
which-builtin-type: "What is the type of this builtin JS value?"
signal-exit: "when you want to fire an event no matter how a process exits."
type-fest: "A collection of essential TypeScript types"
parse-srcset: "A spec-conformant JavaScript parser for the HTML5 srcset attribute"
minipass-flush: "A Minipass stream that calls a flush function before emitting 'end'"
simple-get: "Simplest way to make http get requests. Supports HTTPS, redirects, gzip/deflate, streams in < 100 lines."
big-integer: "An arbitrary length integer library for Javascript"
google-gax: "Google API Extensions"
chokidar: "Minimal and efficient cross-platform file watching library"
lop: "Create parsers using parser combinators with helpful error messages"
chalk: "Terminal string styling done right"
denque: "The fastest javascript implementation of a double-ended queue. Used by the official Redis, MongoDB, MariaDB & MySQL libraries for Node.js and many other libraries. Maintains compatability with deque."
concat-map: "concatenative mapdashery"
env-paths: "Get paths for storing things like data, config, cache, etc"
path-parse: "Node.js path.parse() ponyfill"
mqtt: "A library for the MQTT protocol"
jmespath: "JMESPath implementation in javascript"
ibm-cloud-sdk-core: "Core functionality to support SDKs generated with IBM's OpenAPI SDK Generator."
shebang-command: "Get the command from a shebang"
nan: "Native Abstractions for Node.js: C++ header for Node 0.8 -> 22 compatibility"
eventsource: "W3C compliant EventSource client for Node.js and browser (polyfill)"
imap: "An IMAP module for node.js that makes communicating with IMAP servers easy"
isstream: "Determine if an object is a Stream"
has-flag: "Check if argv has a specific flag"
oauth-1.0a: "OAuth 1.0a Request Authorization for Node and Browser."
rss-parser: "A lightweight RSS parser, for Node and the browser"
lodash.once: "The lodash method `_.once` exported as a module."
html-to-text: "Advanced html to plain text converter"
concat-stream: "writable stream that concatenates strings or binary data and calls a callback with the result"
shimmer: "Safe(r) monkeypatching for JavaScript."
toposort: "Topological sort of directed ascyclic graphs (like dependecy lists)"
async-mutex: "A mutex for guarding async workflows"
prom-client: "Client for prometheus"
winston-transport: "Base stream implementations for winston@3 and up."
help-me: "Help command for node, partner of minimist and commist"
rw: "Now stdin and stdout are files."
glob-parent: "Extract the non-magic parent path from a glob string."
uuid: "RFC9562 UUIDs"
duck: "Rich matchers inspired by Hamcrest. Useful for generating helpful assertion failure messages in tests."
uuencode: "Node.js implementation of the Unix program uuencode."
cssstyle: "CSSStyleDeclaration Object Model implementation"
utf8: "A well-tested UTF-8 encoder/decoder written in JavaScript."
serialize-javascript: "Serialize JavaScript to a superset of JSON that includes regular expressions and functions."
libbase64: "Encode and decode base64 encoded strings"
kuler: "Color your terminal using CSS/hex color codes"
define-data-property: "Define a data property on an object. Will fall back to assignment in an engine without descriptors."
pyodide: "The Pyodide JavaScript package"
google-auth-library: "Google APIs Authentication Client Library for Node.js"
redis-parser: "Javascript Redis protocol (RESP) parser"
retry-request: "Retry a request."
mailparser: "Parse e-mails"
end-of-stream: "Call a callback when a readable/writable/duplex stream has completed or failed."
fs.realpath: "Use node's fs.realpath, but fall back to the JS implementation if the native one fails"
ini: "An ini encoder/decoder for node"
combined-stream: "A stream that emits multiple other streams one after another."
sb-scandir: "File scanning module for Node.js"
expr-eval: "Mathematical expression evaluator"
whatwg-mimetype: "Parses, serializes, and manipulates MIME types, according to the WHATWG MIME Sniffing Standard"
mssql: "Microsoft SQL Server client for Node.js."
node-domexception: "An implementation of the DOMException class from NodeJS"
pdf-parse: "Pure javascript cross-platform module to extract text from PDFs."
url-parse: "Small footprint URL parser that works seamlessly across Node.js and browser environments"
agent-base: "Turn a function into an `http.Agent` instance"
side-channel-map: "Store information about any JS value in a side channel, using a Map"
fill-range: "Fill in a range of numbers or letters, optionally passing an increment or `step` to use, or create a regex-compatible range with `options.toRegex`"
crypto-js: "JavaScript library of crypto standards."
undici-types: "A stand-alone types package for Undici"
util: "Node.js's util module for all engines"
n8n-core: "Core functionality of n8n"
redis: "A modern, high performance Redis client"
flatted: "A super light and fast circular JSON parser."
css-select: "a CSS selector compiler/engine"
p-queue: "Promise queue with concurrency control"
sentence-case: "Transform into a lower case with spaces between words, then capitalize the string"
tar: "tar for node"
get-caller-file: ""
accepts: "Higher-level content negotiation"
rfdc: "Really Fast Deep Clone"
luxon: "Immutable date wrapper"
wordwrap: "Wrap those words. Show them at what columns to start and stop."
domhandler: "Handler for htmlparser2 that turns pages into a dom"
data-urls: "Parses data: URLs"
negotiator: "HTTP content negotiation"
ioredis: "A robust, performance-focused and full-featured Redis client for Node.js."
psl: "Domain name parser based on the Public Suffix List"
is-stream: "Check if something is a Node.js stream"
asynckit: "Minimal async jobs utility library, with streams support"
neo-async: "Neo-Async is a drop-in replacement for Async, it almost fully covers its functionality and runs faster "
sshpk: "A library for finding and using SSH public keys"
semver: "The semantic version parser used by npm."
which-collection: "Which kind of Collection (Map, Set, WeakMap, WeakSet) is this JavaScript value? Works cross-realm, without `instanceof`, and despite Symbol.toStringTag."
convict: "Featureful configuration management library for Node.js (nested structure, schema validation, etc.)"
jwa: "JWA implementation (supports all JWS algorithms)"
bull: "Job manager"
get-package-type: "Determine the `package.json#type` which applies to a location"
is-string: "Is this value a JS String object or primitive? This module works cross-realm/iframe, and despite ES6 @@toStringTag."
jsonpointer: "Simple JSON Addressing."
date-fns-tz: "Time zone support for date-fns v2 with the Intl API"
gm: "GraphicsMagick and ImageMagick for node.js"
cssfilter: "Sanitize untrusted CSS with a configuration specified by a Whitelist. 根据白名单过滤CSS"
protobufjs: "Protocol Buffers for JavaScript (& TypeScript)."
tslib: "Runtime library for TypeScript helper functions"
yallist: "Yet Another Linked List"
asn1.js-rfc5280: "RFC5280 extension structures for asn1.js"
jsonschema: "A fast and easy to use JSON Schema validator"
filelist: "Lazy-evaluating list of files, based on globs or regex patterns"
is-docker: "Check if the process is running inside a Docker container"
foreground-child: "Run a child as if it's the foreground process. Give it stdio. Exit when it exits."
async-function: "A function that returns the normally hidden `AsyncFunction` constructor"
safe-array-concat: "`Array.prototype.concat`, but made safe by ignoring Symbol.isConcatSpreadable"
toidentifier: "Convert a string of words to a JavaScript identifier"
is-data-view: "Is this value a JS DataView? This module works cross-realm/iframe, does not depend on `instanceof` or mutable properties, and despite ES6 Symbol.toStringTag."
lodash.get: "The lodash method `_.get` exported as a module."
bcrypt-pbkdf: "Port of the OpenBSD bcrypt_pbkdf function to pure JS"
lodash.isarguments: "The lodash method `_.isArguments` exported as a module."
call-bind: "Robustly `.call.bind()` a function"
streamsearch: "Streaming Boyer-Moore-Horspool searching for node.js"
deepmerge: "A library for deep (recursive) merging of Javascript objects"
simple-concat: "Super-minimalist version of `concat-stream`. Less than 15 lines!"
sha.js: "Streamable SHA hashes in pure javascript"
selderee: "Selectors decision tree - choose matching selectors, fast"
ee-first: "return the first event in a set of ee/event pairs"
mime-db: "Media Type Database"
promise-ftp: "a promise-based ftp client for node.js"
worker-timers-worker: "The worker which is used by the worker-timers package."
http-errors: "Create HTTP error objects"
no-case: "Transform into a lower cased string with spaces between words"
is-number-object: "Is this value a JS Number object? This module works cross-realm/iframe, and despite ES6 @@toStringTag."
number-allocator: "A library for the unique number allocator"
worker-timers-broker: "The broker which is used by the worker-timers package."
path-is-absolute: "Node.js 0.12 path.isAbsolute() ponyfill"
pg-minify: "Minifies PostgreSQL scripts."
ssh2: "SSH2 client and server modules written in pure JavaScript for node.js"
css-what: "a CSS selector parser"
util-deprecate: "The Node.js `util.deprecate()` function with browser support"
homedir-polyfill: "Node.js os.homedir polyfill for older versions of node.js."
openapi-types: "Types for OpenAPI documents."
cron: "Cron jobs for your node"
posthog-node: "PostHog Node.js integration"
jssha: "jsSHA implements the complete Secure Hash Standard (SHA) family (SHA-1, SHA-224/256/384/512, SHA3-224/256/384/512, SHAKE128/256, cSHAKE128/256, and KMAC128/256) with HMAC"
pg-pool: "Connection pool for node-postgres"
cheerio-select: "CSS selector engine supporting jQuery selectors"
follow-redirects: "HTTP and HTTPS modules that follow redirects."
run-parallel: "Run an array of functions in parallel"
acorn-import-attributes: "Support for import attributes in acorn"
uc.micro: "Micro subset of unicode data files for markdown-it projects."
is-potential-custom-element-name: "Check whether a given string matches the `PotentialCustomElementName` production as defined in the HTML Standard."
which: "Like which(1) unix command. Find the first instance of an executable in the PATH."
has-bigints: "Determine if the JS environment has BigInt support."
boolbase: "two functions: One that returns true, one that returns false"
tmp: "Temporary file and directory creator"
fs-constants: "Require constants across node and the browser"
content-disposition: "Create and parse Content-Disposition header"
snake-case: "Transform into a lower case string with underscores between words"
side-channel-list: "Store information about any JS value in a side channel, using a linked list"
ecdsa-sig-formatter: "Translate ECDSA signatures between ASN.1/DER and JOSE-style concatenation"
pascal-case: "Transform into a string of capitalized words without separators"
possible-typed-array-names: "A simple list of possible Typed Array names."
type-of-is: "Determine and test types using constructor or {}.toString"
dezalgo: "Contain async insanity so that the dark pony lord doesn't eat souls"
default-browser: "Get the default browser"
is-generator-function: "Determine if a function is a native generator function."
media-typer: "Simple RFC 6838 media type parser and formatter"
join-component: "Join a list"
is-lambda: "Detect if your code is running on an AWS Lambda server"
text-hex: "Generate a hex color from the given text"
statuses: "HTTP status utility"
humanize-ms: "transform humanize time to ms"
jsdom: "A JavaScript implementation of many web standards"
aws4: "Signs and prepares requests using AWS Signature Version 4"
ts-type: "add some typescript type and re-export some build-in typescript type"
mustache: "Logic-less {{mustache}} templates with JavaScript"
lru-cache: "A cache object that deletes the least-recently-used items."
randombytes: "random bytes from browserify stand alone"
parse5-parser-stream: "Streaming HTML parser with scripting support."
async: "Higher-order functions and common patterns for asynchronous code"
header-case: "Transform into a dash separated string of capitalized words"
fecha: "Date formatting and parsing"
body-parser: "Node.js body parsing middleware"
tar-fs: "filesystem bindings for tar-stream"
parseurl: "parse a url with memoization"
has-symbols: "Determine if the JS environment has Symbol support. Supports spec, or shams."
wide-align: "A wide-character aware text alignment function for use on the console or with fixed width fonts."
ts-ics: "Create and parse ICS format for TypeScript"
snowflake-sdk: "Node.js driver for Snowflake"
postgres-interval: "Parse Postgres interval columns"
sqlite3: "Asynchronous, non-blocking SQLite3 bindings"
shelljs: "Portable Unix shell commands for Node.js"
undici: "An HTTP/1.1 client, written from scratch for Node.js"
lodash.isinteger: "The lodash method `_.isInteger` exported as a module."
decompress-response: "Decompress a HTTP response if needed"
abbrev: "Like ruby's abbrev module, but in js"
sb-promise-queue: "Promise queue with a nice API"
module-details-from-path: "Extract the Node.js module details like name and base path given an abosulte path to a file inside the module"
assert-plus: "Extra assertions on top of node's assert module"
color-name: "A list of color names and its values"
n8n-editor-ui: "Workflow Editor UI for n8n"
typedarray: "TypedArray polyfill for old browsers"
fastest-levenshtein: "Fastest Levenshtein distance implementation in JS."
is-boolean-object: "Is this value a JS Boolean? This module works cross-realm/iframe, and despite ES6 @@toStringTag."
tedious: "A TDS driver, for connecting to MS SQLServer databases."
ms: "Tiny millisecond conversion utility"
deep-eql: "Improved deep equality testing for Node.js and the browser."
node-ssh: "SSH2 with Promises"
sprintf-js: "JavaScript sprintf implementation"
pump: "pipe streams together and close all of them if one of them closes"
class-validator: "Decorator-based property validation for classes."
extend: "Port of jQuery.extend for node.js and the browser"
bignumber.js: "A library for arbitrary-precision decimal and non-decimal arithmetic"
is-wsl: "Check if the process is running inside Windows Subsystem for Linux (Bash on Windows)"
p-retry: "Retry a promise-returning or async function"
globalthis: "ECMAScript spec-compliant polyfill/shim for `globalThis`"
avsc: "Avro for JavaScript"
err-code: "Create an error with a code"
source-map: "Generates and consumes source maps"
string-width: "Get the visual width of a string - the number of columns required to display it"
postgres-array: "Parse postgres array columns"
n8n-workflow: "Workflow base code of n8n"
rc: "hardwired configuration loader"
bowser: "Lightweight browser detector"
fresh: "HTTP response freshness testing"
events: "Node's event emitter for all engines."
js-base64: "Yet another Base64 transcoder in pure-JS"
esprima-next: "ECMAScript parsing infrastructure for multipurpose analysis"
express-openapi-validator: "Automatically validate API requests and responses with OpenAPI 3 and Express."
typedarray-dts: "TypeScript type definitions for JS TypedArrays"
xtend: "extend like a boss"
p-lazy: "Create a lazy promise that defers execution until it's awaited or when `.then()` or `.catch()` is called"
whatwg-fetch: "A window.fetch polyfill."
destroy: "destroy a stream if possible"
mkdirp: "Recursively mkdir, like `mkdir -p`"
compressible: "Compressible Content-Type / mime checking"
side-channel-weakmap: "Store information about any JS value in a side channel. Uses WeakMap if available."
buffer-equal-constant-time: "Constant-time comparison of Buffers"
is-date-object: "Is this value a JS Date object? This module works cross-realm/iframe, and despite ES6 @@toStringTag."
http-proxy-agent: "An HTTP(s) proxy `http.Agent` implementation for HTTP"
get-system-fonts: "List full paths to all system fonts"
xlsx: "SheetJS Spreadsheet data parser and writer"
helmet: "help secure Express/Connect apps with various HTTP headers"
rrule: "JavaScript library for working with recurrence rules for calendar dates."
cross-fetch: "Universal WHATWG Fetch API for Node, Browsers and React Native"
data-view-buffer: "Get the ArrayBuffer out of a DataView, robustly."
promise-ftp-common: "error classes and other common code used in promise-ftp and promise-sftp"
cookie: "HTTP server cookie parsing and serialization"
rhea: "reactive AMQP 1.0 library"
cacache: "Fast, fault-tolerant, cross-platform, disk-based, data-agnostic, content-addressable cache."
run-applescript: "Run AppleScript and get the result"
rrweb-cssom: "CSS Object Model implementation and CSS parser"
binary-extensions: "List of binary file extensions"
node-rsa: "Node.js RSA library"
has-tostringtag: "Determine if the JS environment has `Symbol.toStringTag` support. Supports spec, or shams."
mongodb: "The official MongoDB driver for Node.js"
is-buffer: "Determine if an object is a Buffer"
multer: "Middleware for handling `multipart/form-data`."
busboy: "A streaming parser for HTML form data for node.js"
bundle-name: "Get bundle name from a bundle identifier (macOS): `com.apple.Safari` → `Safari`"
reflect-metadata: "Polyfill for Metadata Reflection API"
mappersmith: "It is a lightweight rest client for node.js and the browser"
otpauth: "One Time Password (HOTP/TOTP) library for Node.js, Deno, Bun and browsers"
ts-toolbelt: "TypeScript's largest utility library"
functions-have-names: "Does this JS environment support the `name` property on functions?"
param-case: "Transform into a lower cased string with dashes between words"
emoji-regex: "A regular expression to match all Emoji-only symbols as per the Unicode Standard."
html-encoding-sniffer: "Sniff the encoding from a HTML byte stream"
file-uri-to-path: "Convert a file: URI to a file path"
ansi-escapes: "ANSI escape codes for manipulating the terminal"
ldapts: "LDAP client"
node-html-parser: "A very fast HTML parser, generating a simplified DOM, with basic element query support."
cli-spinners: "Spinners for use in the terminal"
es-get-iterator: "Get an iterator for any JS language value. Works robustly across all environments, all versions."
string.prototype.trim: "ES5 spec-compliant shim for String.prototype.trim"
stop-iteration-iterator: "Firefox 17-26 iterators throw a StopIteration object to indicate \"done\". This normalizes it."
pretty-bytes: "Convert bytes to a human readable string: 1337 → 1.34 kB"
flat: "Take a nested Javascript object and flatten it, or unflatten an object with delimited keys"
ansis: "ANSI color lib"
pg-connection-string: "Functions for dealing with a PostgresSQL connection string"
ono: "Throw better errors."
is-regex: "Is this value a JS regex? Works cross-realm/iframe, and despite ES6 @@toStringTag"
smart-buffer: "smart-buffer is a Buffer wrapper that adds automatic read & write offset tracking, string operations, data insertions, and more."
zod-to-json-schema: "Converts Zod schemas to Json Schemas"
yup: "Dead simple Object schema validation"
is-shared-array-buffer: "Is this value a JS SharedArrayBuffer?"
showdown: "A Markdown to HTML converter written in Javascript"
serve-static: "Serve static files"
jszip: "Create, read and edit .zip files with JavaScript http://stuartk.com/jszip"
p-timeout: "Timeout a promise after a specified amount of time"
js-tiktoken: "JavaScript port of tiktoken"
fs-minipass: "fs read and write streams based on minipass"
tunnel-agent: "HTTP proxy tunneling agent. Formerly part of mikeal/request, now a standalone module."
cookie-signature: "Sign and unsign cookies"
teeny-request: "Like request, but smaller."
once: "Run a function exactly one time"
source-map-js: "Generates and consumes source maps"
is-number: "Returns true if a number or string value is a finite number. Useful for regex matches, parsing, user input, etc."
mysql2: "fast mysql driver. Implements core protocol, prepared statements, ssl and compression in native JS"
nwsapi: "Fast CSS Selectors API Engine"
cache-manager: "Cache module for Node.js"
lodash: "Lodash modular utilities."
reflect.getprototypeof: "An ES2015 mostly-spec-compliant `Reflect.getPrototypeOf` sham/polyfill/replacement that works in as many engines as possible"
path-key: "Get the PATH environment variable key cross-platform"
moment: "Parse, validate, manipulate, and display dates"
eventemitter3: "EventEmitter3 focuses on performance while maintaining a Node.js AND browser compatible interface."
browser-request: "Browser port of the Node.js 'request' package"
is-weakset: "Is this value a JS WeakSet? This module works cross-realm/iframe, and despite ES6 @@toStringTag."
basic-auth: "node.js basic auth parser"
array-hyper-unique: "Get unique values of an array. Really, like deeply unique."
token-types: "Common token types for decoding and encoding numeric and string values"
playwright: "A high-level API to automate web browsers"
typed-array-length: "Robustly get the length of a Typed Array"
symbol-tree: "Turn any collection of objects into its own efficient tree or linked list using Symbol"
temp: "Temporary files and directories"
pseudomap: "A thing that is a lot like ES6 `Map`, but without iterators, for use in environments where `for..of` syntax and `Map` are not available."
infer-owner: "Infer the owner of a path based on the owner of its nearest existing parent"
express-async-errors: "Async/await error handling support for expressjs"
msgpackr: "Ultra-fast MessagePack implementation with extensions for records and structured cloning"
require-from-string: "Require module from string"
es-set-tostringtag: "A helper to optimistically set Symbol.toStringTag, when possible."
require-in-the-middle: "Module to hook into the Node.js require function"
form-data: "A library to create readable \"multipart/form-data\" streams. Can be used to submit forms and file uploads to other web applications."
redis-errors: "Error classes used in node_redis"
math-intrinsics: "ES Math-related intrinsics and helpers, robustly cached."
fast-unique-numbers: "A module to create a set of unique numbers as fast as possible."
safe-stable-stringify: "Deterministic and safely JSON.stringify to quickly serialize JavaScript objects"
array-series: "Call an array of asynchronous functions in series"
object-assign: "ES2015 `Object.assign()` ponyfill"
type-is: "Infer the content-type of a request."
remove-trailing-slash: "removes trailing slashes"
fast-xml-parser: "Validate XML, Parse XML, Build XML without C/C++ based libraries"
supports-color: "Detect whether a terminal supports color"
es-errors: "A simple cache for a few of the JS Error constructors."
file-type: "Detect the file type of a Buffer/Uint8Array/ArrayBuffer"
epub2: "Parse ePub electronic book files with Node.JS"
isexe: "Minimal module to check if a file is executable."
lodash.includes: "The lodash method `_.includes` exported as a module."
function.prototype.name: "An ES2015 spec-compliant `Function.prototype.name` shim"
leac: "Lexer / tokenizer"
replacestream: "A node.js through stream that does basic streaming text search and replace and is chunk boundary friendly"
expand-tilde: "Bash-like tilde expansion for node.js. Expands a leading tilde in a file path to the user home directory, or `~+` to the cwd."
finalhandler: "Node.js final http responder"
validator: "String validation and sanitization"
tweetnacl-util: "String encoding utilitlies extracted from TweetNaCl.js"
is-set: "Is this value a JS Set? This module works cross-realm/iframe, and despite ES6 @@toStringTag."
component-type: "Cross-browser type assertions (less broken typeof)"
object-inspect: "string representations of objects in node and the browser"
stubs: "Easy method stubber."
ws: "Simple to use, blazing fast and thoroughly tested websocket client and server for Node.js"
fastq: "Fast, in memory work queue"
minizlib: "A small fast zlib stream built on [minipass](http://npm.im/minipass) and Node.js's zlib binding."
inflight: "Add callbacks to requests in flight to avoid async duplication"
d3-dsv: "A parser and formatter for delimiter-separated values, such as CSV and TSV"
compression: "Node.js compression middleware"
is-weakmap: "Is this value a JS WeakMap? This module works cross-realm/iframe, and despite ES6 @@toStringTag."
strnum: "Parse String to Number based on configuration"
qs: "A querystring parser that supports nesting and arrays, with a depth limit"
event-target-shim: "An implementation of WHATWG EventTarget interface."
safe-buffer: "Safer Node.js Buffer API"
make-fetch-happen: "Opinionated, caching, retrying fetch client"
regenerator-runtime: "Runtime for Regenerator-compiled generator and async functions."
rfc2047: "Encode and decode rfc2047 (MIME encoded words)"
duplexify: "Turn a writable and readable stream into a streams2 duplex stream with support for async initialization and streams1/streams2 input"
peberminta: "Simple, transparent parser combinators toolkit that supports any tokens"
minimatch: "a glob matcher in javascript"
deep-equal: "node's assert.deepEqual algorithm"
memory-pager: "Access memory using small fixed sized buffers"
url-join: "Join urls and normalize as in path.join."
has-proto: "Does this environment have the ability to get the [[Prototype]] of an object on creation with `__proto__`?"
callsites: "Get callsites from the V8 stack trace API"
app-root-path: "Determine an app's root path from anywhere inside the app"
camel-case: "Transform into a string with the separator denoted by the next word capitalized"
jq: error (at /usr/local/lib/node_modules/n8n/node_modules/buffer-from/package.json:19): null (null) cannot be matched, as it is not a string
formdata-node: "Spec-compliant FormData implementation for Node.js"
class-transformer: "Proper decorator-based transformation / serialization / deserialization of plain javascript objects to class constructors"
setimmediate: "A shim for the setImmediate efficient script yielding API"
async-retry: "Retrying made simple, easy and async"
is-retry-allowed: "Check whether a request can be retried based on the `error.code`"
methods: "HTTP methods that node supports"
which-typed-array: "Which kind of Typed Array is this JavaScript value? Works cross-realm, without `instanceof`, and despite Symbol.toStringTag."
define-properties: "Define multiple non-enumerable properties at once. Uses `Object.defineProperty` when available; falls back to standard assignment in older engines."
fast-uri: "Dependency-free RFC 3986 URI toolbox"
transliteration: "Unicode to ACSII transliteration / slugify module for node.js, browser, Web Worker, ReactNative and CLI."
crypt: "utilities for encryption and hashing"
encoding-sniffer: "Implementation of the HTML encoding sniffer algo, with stream support"
punycode: "A robust Punycode converter that fully complies to RFC 3492 and RFC 5891, and works on nearly all JavaScript platforms."
ignore: "Ignore is a manager and filter for .gitignore rules, the one used by eslint, gitbook and many others."
immediate: "A cross browser microtask library"
express-rate-limit: "Basic IP rate-limiting middleware for Express. Use to limit repeated requests to public APIs and/or endpoints such as password reset."
deep-extend: "Recursive object extending"
gauge: "A terminal based horizontal gauge"
xml-name-validator: "Validates whether a string matches the production for an XML name or qualified name"
is-weakref: "Is this value a JS WeakRef? This module works cross-realm/iframe, and despite ES6 @@toStringTag."
function-bind: "Implementation of Function.prototype.bind"
simple-lru-cache: ""
is-array-buffer: "Is this value a JS ArrayBuffer?"
moment-timezone: "Parse and display moments in any timezone."
express: "Fast, unopinionated, minimalist web framework"
parseley: "CSS selectors parser"
brace-expansion: "Brace expansion as known from sh/bash"
mqtt-packet: "Parse and generate MQTT packets like a breeze"
tarn: "Simple and robust resource pool for node.js"
yaml: "JavaScript parser and stringifier for YAML"
minimist: "parse argument options"
generic-pool: "Generic resource pooling for Node.JS"
rechoir: "Require any supported file as a node module."
zod: "TypeScript-first schema declaration and validation library with static type inference"
jq: error (at /usr/local/lib/node_modules/n8n/node_modules/binascii/package.json:15): null (null) cannot be matched, as it is not a string
hexoid: "A tiny (190B) and extremely fast utility to generate random IDs of fixed length"

```
